# 09 — UI (UMG / CommonUI)

Parent: [00_INDEX.md](./00_INDEX.md)

## Loop

```text
find existing widget pattern → extend → bind to existing data
  → compile → human layout/input gate → memory
```

## Rules

1. Match project UI framework (UMG vs CommonUI) from fingerprint
2. Reuse style / common actuators; no new design system unless asked
3. Safe zones / gamepad: follow existing HUD patterns
4. Visual done = human gate (MD-only)

## Verify

- Widget compiles · referenced assets valid · human UI pass
